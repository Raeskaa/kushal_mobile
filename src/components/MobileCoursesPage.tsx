import { useState } from 'react';

interface MobileCoursesPageProps {
  activeTab: string;
}

export function MobileCoursesPage({ activeTab }: MobileCoursesPageProps) {
  return (
    <div className="min-h-screen bg-white">
      {/* Empty content - navigation only */}
    </div>
  );
}