import { useState } from 'react';

interface MobileHomeDashboardProps {
  activeTab: string;
}

export function MobileHomeDashboard({ activeTab }: MobileHomeDashboardProps) {
  return (
    <div className="min-h-screen bg-white">
      {/* Empty content - navigation only */}
    </div>
  );
}