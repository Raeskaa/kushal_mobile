import { useState } from 'react';

interface MobileEventsPageProps {
  activeTab: string;
}

export function MobileEventsPage({ activeTab }: MobileEventsPageProps) {
  return (
    <div className="min-h-screen bg-white">
      {/* Empty content - navigation only */}
    </div>
  );
}