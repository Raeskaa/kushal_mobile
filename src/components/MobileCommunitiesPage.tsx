import { useState } from 'react';

interface MobileCommunitiesPageProps {
  activeTab: string;
}

export function MobileCommunitiesPage({ activeTab }: MobileCommunitiesPageProps) {
  return (
    <div className="min-h-screen bg-background">
      {/* Empty content - navigation only */}
    </div>
  );
}